name: Android Build

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'apps/android/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
        api-level: 34
        build-tools: 34.0.0

      - name: Debug - List directory structure
        run: ls -la

      - name: Debug - Check apps directory
        run: ls -la apps/ || echo "apps directory not found"

      - name: Build debug APK
        working-directory: apps/android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace
          echo "=== Build Output Directory ==="
          find . -name "*.apk" -type f 2>/dev/null
          echo "=== APK Location Check ==="
          ls -la app/build/outputs/apk/debug/ 2>/dev/null || echo "APK directory not found"

      - name: Build release APK
        working-directory: apps/android
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon --stacktrace
          echo "=== Build Output Directory ==="
          find . -name "*.apk" -type f 2>/dev/null
          echo "=== APK Location Check ==="
          ls -la app/build/outputs/apk/debug/ 2>/dev/null || echo "APK directory not found"

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: openclaw-2026.2.20-debug.apk
          path: apps/android/app/build/outputs/apk/debug/openclaw-2026.2.20-debug.apk

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: openclaw-2026.2.20-release.apk
          path: apps/android/app/build/outputs/apk/release/openclaw-2026.2.20-release.apk
